# Example
schedules:
 - name: template-zexample-scheduler
   cron_schedule: "10 7 * * MON"
   description: "Example scheduler"
   select: "tag:zexample"
   time_zone: "America/New_York"
   dbt_profile: prd

 - name: template-zexample-scheduler
   cron_schedule: "10 7 1 * "
   description: "Example scheduler"
   select: "tag:zexample"
   time_zone: "America/New_York"
   dbt_profile: acc

 - name: template-zexample-scheduler
   cron_schedule: "10 7 1 1 *"
   description: "Example scheduler"
   select: "tag:zexample"
   time_zone: "America/New_York"
   dbt_profile: dev