version: 2

sources:
  - name: rand-rusaweb
    project: | 
      {%- if target.name == "prd_target" -%} us-ergbq-prd-3693
      {%- elif target.name == "acc_target" -%} us-ergbq-qa-e523
      {%- else -%} rand-rusaweb
      {%- endif -%}
    schema: dims
    tables:
      - name: DIM_DATE
      - name: eBI_Fiscal_Calendar
      - name: DIM_DEPT
      - name: DIM_WEEK
      - name: v_Dim_Branch
      - name: vw_Week_Start_And_End_Dates
